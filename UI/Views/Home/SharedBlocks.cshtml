@{
    ViewData["Title"] = "Shared Blocks";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h2>Shared Blocks</h2>
            <p class="text-muted">Discover and fork blocks shared by the community</p>
        </div>
    </div>

    <div class="text-center py-5">
        <i class="bi bi-gear" style="font-size: 3rem; color: #6c757d;"></i>
        <h3 class="mt-3">Coming Soon</h3>
        <p class="text-muted">This feature is under development. Soon you'll be able to browse, star, and fork blocks shared by other users!</p>
        <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Create Your Own Block</a>
    </div>
</div>

<!-- Bootstrap Icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
